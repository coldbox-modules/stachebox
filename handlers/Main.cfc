component extends="coldbox.system.EventHandler" {

	function index( event, rc, prc ){

		// for( var i=1; i<=100; i++ ){
		// 	try{
		// 		throw( "Error #i# thrown!" );
		// 	} catch( any e ){
		// 		log.error(
		// 			"Error calling #arguments.event.getCurrentEvent()#: #e.message# #e.detail#",
		// 			{ "exception" : e }
		// 		);
		// 	}
		// }

	}

}